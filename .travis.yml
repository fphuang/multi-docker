sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t fphuang/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true fphuang/react-test npm test

after_success:
  - docker build -t fphuang/multi-client ./client
  - docker build -t fphuang/multi-nginx ./nginx
  - docker build -t fphuang/multi-server ./server
  - docker build -t fphuang/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push fphuang/multi-client
  - docker push fphuang/multi-nginx
  - docker push fphuang/multi-server
  - docker push fphuang/multi-worker
